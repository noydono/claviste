<!-- Modal add commentaire-->
<form action="/commentaire/create/{{dbArticleSingle._id}}" method="POST">
    <div id="modalAddCom{{dbArticleSingle._id}}" class="modal  modal-fixed-footer" style="height: 30%;" >
        <div class="modal-content">
            <div class="row center-align">
                <span class="title" style="margin-top: 10px; margin-bottom:25px;">Ajouter un commentaire</span>
            </div>
            <div class="row">
                    <div class="row">
                        <div class="input-field col s12">
                            <textarea id="textarea{{dbArticleSingle._id}}" class="materialize-textarea" name="content"></textarea>
                            <label for="textarea{{dbArticleSingle._id}}">Contenu du commentaire</label>
                        </div>
                    </div>
            </div>
        </div>
        <div class="modal-footer">
            <a href="#!" class="modal-close waves-effect waves-green btn left">Fermé</a>
            <button type="submit"  class="modal-close waves-effect waves-green btn">Envoyer</button>
        </div>
    </div>
</form>
