<!-- Modal add commentaire-->
<form action="/commentaire/create/{{dbArticleSingle._id}}" method="POST">
    <div id="modalAddCom{{dbArticleSingle._id}}" class="modal  modal-fixed-footer" style="height: 30%;" >
        <div class="modal-content">
            <div class="row center-align">
                <span class="title" style="margin-top: 10px; margin-bottom:25px;">ajoute un commmentaire</span>
            </div>
            <div class="row">
                    <div class="row">
                        <div class="input-field col s12">
                            <textarea id="textarea{{dbArticleSingle._id}}" class="materialize-textarea" name="content"></textarea>
                            <label for="textarea{{dbArticleSingle._id}}">ICI c'est le texte</label>
                        </div>
                    </div>
            </div>
        </div>
        <div class="modal-footer">
            <a href="#!" class="modal-close waves-effect waves-green btn left">Close</a>
            <button type="submit" href="#!" class="modal-close waves-effect waves-green btn">envoyer</button>
        </div>
    </div>
</form>
